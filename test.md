# 无人机运动控制调试手册

本文档仅记录 STM32 下位机的运动控制参数及调整效果，不包含 OpenMV 曝光、颜色阈值、ROI 或视觉滤波参数。

> 安全提示：首次调试指令与串口数据时应拆下螺旋桨。实飞调试时应从小速度、小位移、宽死区开始，并保留遥控器人工接管能力。

## 1. 当前自主任务流程

运控状态机位于 `FcSrc/User_Task.c`，由 50Hz 任务调用，每 20ms 执行一次。

```text
切换程控模式
  ↓
解锁
  ↓
等待2秒
  ↓
起飞到100cm
  ↓
等待5秒
  ↓
分步向前移动
  ↓
收到转弯信号
  ↓
悬停稳定
  ↓
十字中心对准
  ↓
统计当前拐角小球
  ↓
左转/右转90°
  ↓
重复4个拐角
  ↓
自动降落
```

## 2. 遥控器任务触发范围

位置：`FcSrc/User_Task.c`

| AUX2 通道值 | 功能 |
|---:|---|
| 800～1200 | 一键降落 |
| 1300～1700 | 一键起飞到 100cm |
| 1700～2200 | 启动完整自主任务 |

这些范围目前直接写在条件判断中，不是宏定义。如果遥控器实际输出不在预期范围，应先校准通道行程，不建议直接大幅放宽代码阈值。

## 3. 起飞与初始稳定参数

| 参数 | 当前值 | 增大后 | 减小后 | 建议 |
|---|---:|---|---|---|
| 一键起飞高度 | 100cm | 视野变大，地面目标变小，风险增加 | 目标变大，但可能视野不完整 | 在 `OneKey_Takeoff(100)` 中调整 |
| 解锁后等待 | 2000ms | 起飞前更稳定，任务启动更慢 | 启动更快，飞控状态可能未完全稳定 | 一般不建议小于 1000ms |
| 起飞后等待 | 5000ms | 首段前进前更稳，任务耗时增加 | 更快进入巡航，但高度/姿态可能未稳定 | 先观察起飞实际收敛时间 |

起飞高度在一键起飞和自主任务中各写了一次。如果需要统一调整，应同时检查两处 `OneKey_Takeoff(100)`。

## 4. 直线前进参数

当前激活的步骤6直接调用：

```c
Horizontal_Move(5, 20, 0);
```

| 参数 | 当前值 | 增大后 | 减小后 |
|---|---:|---|---|
| 单次前进距离 | 5cm | 前进效率提高，但可能错过转弯点，停车距离增大 | 运动更细致，指令次数增加，整体速度降低 |
| 前进速度 | 20cm/s | 任务更快，但制动距离、姿态波动和过冲增加 | 运动更稳，但耗时增加 |
| 方向角 | 0° | 0°为机头前方，90°为右，180°为后，270°为左 | 不应用增减思路调节，应按期望方向设置 |

### 重要说明

`LINE_MOVE_DISTANCE_CM=5` 和 `LINE_MOVE_SPEED_CMPS=30` 虽然已定义，但当前激活的步骤6没有使用这两个宏，而是使用上述硬编码的 `5, 20, 0`。只修改这两个宏不会改变当前实际飞行效果。

## 5. 转弯信号确认

当前要求连续 3 次检测到有效转弯方向，才从直线前进进入悬停。

| 项目 | 当前值 | 调大后 | 调小后 |
|---|---:|---|---|
| 连续确认次数 | 3次 | 误转弯减少，但飞机会更晚停下 | 转弯反应更快，但单次异常数据可能触发转弯 |

该数值目前由代码中的 `turn_signal_cnt >= 3` 硬编码控制。

调整建议：

- 飞机经常在直线上误停：先检查接收数据，再考虑增加确认次数。
- 飞机总是越过拐角才停：先减小前进距离/速度，再考虑将确认次数降到 2。

## 6. 悬停稳定时间

```c
#define HOVER_STABLE_TIME 1000
```

| 调整 | 效果 |
|---|---|
| 增大 | 飞机在对准前有更多时间制动和稳定，但每个拐角耗时增加 |
| 减小 | 任务更快，但飞机可能带着水平速度进入中心对准 |

如果进入对准阶段后首次校正幅度很大，应优先增大悬停时间，并检查悬停指令是否已发送成功。

## 7. 十字中心对准参数

```c
#define CROSS_X_DEAD_ZONE        30
#define CROSS_Y_DEAD_ZONE        20
#define CROSS_ADJUST_DIST_CM     2
#define CROSS_ADJUST_SPEED_CMPS  20
#define CROSS_STABLE_TIME_MS     200
```

| 参数 | 默认值 | 增大后 | 减小后 |
|---|---:|---|---|
| `CROSS_X_DEAD_ZONE` | 30px | 横向更容易判定完成，允许的最终误差更大 | 横向更精确，但可能左右震荡 |
| `CROSS_Y_DEAD_ZONE` | 20px | 纵向更容易判定完成 | 纵向更精确，但可能前后震荡 |
| `CROSS_ADJUST_DIST_CM` | 2cm | 单次校正更快，但容易越过中心 | 单次校正更细，收敛时间增加 |
| `CROSS_ADJUST_SPEED_CMPS` | 20cm/s | 对准更快，过冲和姿态波动增加 | 更稳定，但对准耗时增加 |
| `CROSS_STABLE_TIME_MS` | 200ms | 需在死区内稳定更久，误通过减少 | 更快进入计数，可能把瞬时穿过死区当作对准完成 |

对准方向当前为：

| 坐标条件 | 运动指令 |
|---|---|
| `cross_y > +20` | 向前 |
| `cross_y < -20` | 向后 |
| `cross_x > +30` | 向右 |
| `cross_x < -30` | 向左 |

如果飞机与目标的距离越调越大，说明坐标符号或机头方向不匹配，不能通过增大死区解决。

如果在中心附近往复震荡，按以下顺序调整：

1. 减小 `CROSS_ADJUST_DIST_CM`。
2. 减小 `CROSS_ADJUST_SPEED_CMPS`。
3. 适当增大对应方向的死区。
4. 如果偶尔穿过死区就进入下一阶段，增大 `CROSS_STABLE_TIME_MS`。

## 8. 拐角小球取样参数

STM32 当前在对准完成后取样 5 帧，分别保留红、绿、蓝三个通道的最大值。

| 参数 | 当前值 | 增大后 | 减小后 |
|---|---:|---|---|
| 取样帧数 | 5帧 | 更容易包含完整识别结果，但取到偶发假数量的概率也增加 | 统计更快，短暂漏检时可能少计 |
| 取样周期 | 约20ms | 周期增大可覆盖更长时间，但飞机停留更久 | 周期减小可能重复读取同一批视觉数据 |

当前取样帧数是代码中的 `ball_sample_count < 5`，不是宏定义。

## 9. 转弯参数

```c
#define TURN_ANGLE         90
#define TURN_SPEED         30
#define TURN_EXECUTE_TIME  10000
#define TOTAL_TURN_TIMES   4
```

| 参数 | 默认值 | 增大后 | 减小后 |
|---|---:|---|---|
| `TURN_ANGLE` | 90° | 实际旋转角度增大 | 实际旋转角度减小 |
| `TURN_SPEED` | 30°/s | 转弯更快，姿态波动和过冲增加 | 转弯更稳，耗时增加 |
| `TURN_EXECUTE_TIME` | 10000ms | 下一段前进开始更晚 | 开始更早，但可能在旋转未完成时就前进 |
| `TOTAL_TURN_TIMES` | 4 | 任务绕过更多拐角后才降落 | 更早降落 |

### 转弯时间设置

当前程序没有根据飞控的“转弯完成”状态结束等待，而是使用固定的 `TURN_EXECUTE_TIME`。

理论转弯时间可估算为：

```text
转弯时间 ≈ TURN_ANGLE / TURN_SPEED
           ≈ 90 / 30
           ≈ 3秒
```

但实际还包含指令响应、加减速和姿态收敛时间，因此 `TURN_EXECUTE_TIME` 必须留有余量。当前 10 秒偏保守。

推荐调试方法：

1. 保持 `TURN_ANGLE=90`、`TURN_SPEED=30`。
2. 记录从发出命令到航向稳定的最长时间。
3. 将 `TURN_EXECUTE_TIME` 设为最长时间再增加约 0.5～1 秒余量。
4. 确认时间后，再单独校准 `TURN_ANGLE`。

## 10. 降落阶段

完成 `TOTAL_TURN_TIMES` 后，程序在步骤11反复调用 `OneKey_Land()`。当前没有在降落完成后显式结束任务状态。

降落调试时应检查：

- 是否只需发送一次降落命令。
- 飞控对重复降落命令的处理是否安全。
- 降落后是否自动锁定。
- AUX2 退出自主档位后，任务状态是否正确清除。

## 11. 遥控手动目标限制

位置：`FcSrc/ANO_LX.c`

| 参数 | 默认值 | 作用 |
|---|---:|---|
| `MAX_ANGLE` | 3500（0.01°） | 最大横滚/俯仰目标约 35° |
| `MAX_YAW_DPS` | 200°/s | 最大偏航角速度 |
| `MAX_VELOCITY` | 500cm/s | 预留的水平速度上限 |
| `MAX_VER_VEL_P` | 300cm/s | 预留的正向垂直速度上限 |
| `MAX_VER_VEL_N` | 200cm/s | 预留的反向垂直速度上限 |

`MAX_ANGLE` 和 `MAX_YAW_DPS` 当前参与遥控摇杆到目标值的换算。其余速度宏在当前激活的遥控处理中没有直接使用，修改前应先确认调用链。

## 12. 当前未启用的运控参数

`OpenMV_Angle_PID_Control()` 当前没有在激活的直线步骤中调用，因此下列参数目前修改后不会改变实际巡航效果：

- `ANGLE_PID_KP`
- `ANGLE_PID_KI`
- `ANGLE_PID_KD`
- `ANGLE_PID_MAX`
- `ANGLE_PID_I_MAX`
- `ANGLE_DEAD_ZONE`
- `LINE_PID_PERIOD_MS`

`CORNER_CONFIRM_TIME` 目前也没有参与激活的转弯判定。

在未恢复 `OpenMV_Angle_PID_Control()` 调用之前，不建议通过调整这些 PID 数值排查直线偏移。

## 13. 运控常见现象与调整方向

| 现象 | 优先检查 | 调整建议 |
|---|---|---|
| 起飞后立即前进，姿态未稳 | 起飞后 5 秒等待 | 增大等待时间 |
| 飞机越过拐角 | 前进步长、速度、转弯确认次数 | 优先减小步长和速度 |
| 直线上偶发停车 | `turn_signal_cnt >= 3` | 确认数据正确后再增大确认次数 |
| 悬停后仍有明显水平速度 | `HOVER_STABLE_TIME` | 适当增大 |
| 对准时左右/前后震荡 | 对准步长和速度 | 先减小步长，再减小速度 |
| 对准方向相反 | 坐标符号和机头方向 | 修正方向映射，不要用死区补偿 |
| 对准长时间不完成 | 死区、步长、飞控定点精度 | 先确认实际悬停波动范围，再设置死区 |
| 转弯未完成就前进 | `TURN_EXECUTE_TIME` | 增大等待时间 |
| 转弯完成后停留过久 | `TURN_EXECUTE_TIME` | 在保留安全余量的前提下减小 |
| 每转都少于/大于 90° | `TURN_ANGLE` | 根据固定方向的重复实验结果小幅校准 |

## 14. 推荐的实飞调试顺序

1. 拆桨验证模式切换、解锁、起飞、移动、转弯和降落指令的发送顺序。
2. 只测试起飞与悬停，确认 100cm 高度和稳定时间。
3. 使用低速度和 2～5cm 步长测试单方向水平移动。
4. 单独测试十字对准，先确认四个方向没有反向。
5. 单独测试左转和右转，记录实际转弯角度和收敛时间。
6. 连接“前进→悬停→对准→转弯”单个拐角。
7. 单拐角稳定后，再将 `TOTAL_TURN_TIMES` 恢复为 4 测试完整任务。

## 15. 调试记录模板

```text
日期/场地：
电池电压：
负载/起飞重量：
起飞高度：

测试阶段：起飞 / 前进 / 对准 / 转弯 / 降落
修改参数：
原值：
新值：

修改前现象：
修改后现象：
是否保留：
备注：
```

每次只修改一个运控参数。电池电压、负载和风力不同时，转弯、制动和对准结果不具有直接可比性。
